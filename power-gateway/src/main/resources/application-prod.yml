# 生产：Nacos 服务发现 + lb://；调试鉴权强制关闭
spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: ${NACOS_ADDR}
        fail-fast: true
    gateway:
      routes:
        - id: power-auth
          uri: lb://power-auth
          predicates:
            - Path=/auth/**
        - id: power-system
          uri: lb://power-system
          predicates:
            - Path=/system/**
        - id: power-workflow
          uri: lb://power-workflow
          predicates:
            - Path=/workflow/**

power:
  security:
    jwt-secret: ${JWT_SECRET}
    debug-auth-enabled: false

logging:
  level:
    root: WARN
    com.power: INFO
